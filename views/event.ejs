<%- include('partials/header') -%>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= event.name %></h2>
      <div class = 'imageContainer'>
        <img id = 'rockets' class="" src="<%= event.feature_image%>" />
      </div>
      
      <div class="row justify-content-between">
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form>
        <h3 class="col-3">Likes: <%= 3 %></h3>
      </div>
    </div>
    <div class="col-3 mt-5">
      <time class="published info" datetime="2015-11-01">
        <%= moment(event.date).format('Do MMMM, YYYY, h:mm:ss a')%>
      </time>
      <p><%= event.description%></p>

    </div>
    <div class="mt-5">
      <h2>Add a comment</h2>
      
    <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/">Return to Feed</a>
    </div>
  </div>
</div>

<%- include('partials/footer') -%>